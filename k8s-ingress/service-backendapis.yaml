apiVersion: v1
kind: Service
metadata:
  name: backendapis-service
  labels:
    app: backendapis
spec:
  type: ClusterIP
  ports:
    - port: 5020
      targetPort: 5020
      protocol: TCP
      name: http
  selector:
    app: backendapis
